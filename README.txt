.NET 8.0

Database: 
_SQLite database (bin/Debug/net8.0/MyDatabase.db)

Libraries:
_ System.Data.SQLite 
_ Microsoft Entity Framework 

Pattern matching algorithm:
_Knuth-Morris-Pratt

This is currently a test: all the CLI is in Italian only and there are bugs that need to be fixed 
Currently working on Windows, to be tested on macOS and Linux

To try the software follow the instruction of the main branch
(the filesystem structure is the same, but you will need to chenge the directory names) 

GPL License V3
